{
  "name": "Movie-Info",
  "version": "0.0.1",
  "description": "A desktop software to search for movie details",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package-win": "electron-packager . --overwrite --asar --platform=win32 --arch=ia32 --icon=assets/win/icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Movie info\"" 
  },
  "keywords": [
    "Electron",
    "Project"
  ],
  "author": "Luteya Coulston",
  "license": "ISC",
  "devDependencies": {
    "electron": "^11.0.4",
    "electron-packager": "^15.2.0"
  }
}
